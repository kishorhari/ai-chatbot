"""Shared pytest configuration and fixtures.

Intentionally minimal for Milestone 1's bootstrap step. Fixtures (settings
overrides, provider builders, respx mocks) are added in later sub-milestones as
the modules under test come into existence.
"""
