"""LLM application contracts — the ``ProviderRegistry`` port.

Resolves a provider by name or returns the configured default (ADR-0002). The
concrete, dict-backed implementation is built by the composition root.
"""
